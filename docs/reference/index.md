# API reference

The public API of `ds_msp`, generated from the source docstrings.

!!! note "Being generated"
    The per-module reference pages are produced by the reference pass during the docs rollout
    (see the [Roadmap](../ROADMAP.md)). They render from NumPy-style docstrings via
    [mkdocstrings](https://mkdocstrings.github.io/).

Planned page set (one per public sub-package):
`core`, `models`, `ops`, `adapt`, `calib`, `io`, `mvg`, `stereo`, `vo`, `cv`, `ldc`.
