# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.env
*.egg-info/
build/
dist/

# Generated Results
results/

# System
.DS_Store

# Verification outputs (regenerate via verify_real_samples.py)
verification_output/

# Public datasets (downloaded via scripts/download_datasets.sh, not committed)
# — but DO track the dataset guide so learners know what to fetch and why.
# (Ignore the dir *contents*, not the dir itself, so the negation can apply.)
datasets/*
!datasets/README.md

# Local-only working notes (kept out of the public repo).
CAREER_ROADMAP.md

# Local-only authoring/tooling config — never committed.
.claude/
.ai/
CLAUDE.md
CLAUDE.local.md

# Internal R&D and planning docs (kept local, never published).
docs/research/
docs/MULTI_MODEL_ADAPTER_PLAN.md
docs/MULTI_MODEL_DEV_PROCESS.md

# Local-only rendering scripts and their heavy viz dependencies; only their committed
# output (under assets/) belongs in this repo.
/simulations/

# Local detection/repro experiments (use datasets, not shipped).
/experiments/
