# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.testmondata
.pytest_cache/
.coverage
.Python
env/
venv/
.venv/
.env
*.egg-info/
build/
dist/

# Generated Results
results/

# System
.DS_Store

# Verification outputs (regenerate via verify_real_samples.py)
verification_output/

# Public datasets (downloaded via scripts/download_datasets.sh, not committed)
# — but DO track the dataset guide so learners know what to fetch and why.
# (Ignore the dir *contents*, not the dir itself, so the negation can apply.)
datasets/*
!datasets/README.md

# Local-only authoring/tooling config — never committed.
.claude/
.ai/
CLAUDE.md
CLAUDE.local.md

# Internal R&D, planning, and working notes (kept local, never published).
docs/research/

# Local calibration datasets (large; downloaded/produced locally, never committed).
2026_*_MC-Calib/

# Local-only rendering scripts and their heavy viz dependencies; only their committed
# output (under assets/) belongs in this repo.
/simulations/

# Local detection/repro experiments (use datasets, not shipped).
/experiments/

# Local-only data/output directories (never committed).
/reports/
# Scratch diagnostics dropped in the repo root during investigations.
/diag_*.py
