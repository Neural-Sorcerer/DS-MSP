# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
.env
*.egg-info/
build/
dist/

# Generated Results
results/

# System
.DS_Store

# Verification outputs (regenerate via verify_real_samples.py)
verification_output/

# Public datasets (downloaded via scripts/download_datasets.sh, not committed)
# — but DO track the dataset guide so learners know what to fetch and why.
# (Ignore the dir *contents*, not the dir itself, so the negation can apply.)
datasets/*
!datasets/README.md

# Personal job-hunt strategy notes (kept local, not for the public repo).
CAREER_ROADMAP.md

# Simulation / rendering studio (LOCAL-ONLY, never committed to this repo).
# It has its own local git repo and heavy viz dependencies (matplotlib, manim, …).
# RULE: the studio generates visual assets; only its OUTPUT (copied into assets/)
# lands here. The generator code and its deps stay out of GitHub entirely.
/simulations/

# Local detection/repro experiments (use datasets, not shipped).
/experiments/
